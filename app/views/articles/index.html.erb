<p style="color: green"><%= notice %></p>

<h1>Articles</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
            <%= link_to article.title, article%> 
          </li>
        <% end %>
        </ul>

      <%= paginate @articles %>
      <%= link_to "New article", new_article_path, class: "btn btn-secondary", style: "margin-top: 10px;" %>
